<% if obj.errors.any? %>
  <h2>Error on saving for <%= obj.class.name.downcase %> </h2>
  <ul>
    <% obj.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
  </ul>
<% end %>