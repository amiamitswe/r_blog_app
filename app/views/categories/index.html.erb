<h1 class="text-center mb-2">All Categories</h1>

<% @categories.each do |category| %>
  <p class="user"><%= link_to category.name, category_path(category) %></p>
<% end %>

<div class="apple_pagination">
  <%= will_paginate @categories, :container => false %>
</div>